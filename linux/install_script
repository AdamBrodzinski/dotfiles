# update the package database
sudo pacman -Syy

# cli tools
sudo pacman -S curl
sudo pacman -S wget
sudo pacman -S less
sudo pacman -S stow
sudo pacman -S ack
sudo pacman -S fd
sudo pacman -S jq
sudo pacman -S tree
sudo pacman -S xsel
sudo pacman -S z
sudo package -S trash-cli

# programming tools
sudo pacman -S git
sudo pacman -S asdf
sudo pacman -S lazygit
sudo pacman -S lazydocker
sudo pacman -S ripgrep
sudo pacman -S neovim

# language support
sudo pacman -S lua
sudo pacman -S opam

# framework13 laptop (arch)
sudo pacman -S bluez bluez-utils
sudo systemctl enable bluetooth

# scaling 1.58

# enable intercept plugin
# https://wiki.archlinux.org/title/Interception-tools
sudo pacman -S interception-tools interception-caps2esc
# systemctl enable udevmon
